---
- name: Create main directory
  ansible.builtin.file:
    path: /mnt/docker/openobserve
    state: directory
    owner: "{{ default_username }}"
    group: "{{ default_username }}"
    mode: "0755"

- name: Create sub directories
  ansible.builtin.file:
    path: /mnt/docker/openobserve/{{ item.path }}
    state: directory
    owner: "{{ default_username }}"
    group: "{{ default_username }}"
    mode: "0755"
  with_community.general.filetree: templates/compose/
  when: item.state == "directory"

- name: Ensure config files exist
  ansible.builtin.template:
    src: "{{ item.src }}"
    dest: /mnt/docker/openobserve/{{ item.path }}
    owner: "{{ default_username }}"
    group: "{{ default_username }}"
    mode: "0644"
  with_community.general.filetree: templates/compose/
  when: item.state == "file"

- name: Ensure openobserve systemd service installed
  ansible.builtin.template:
    src: systemd/openobserve.service
    dest: /etc/systemd/system/docker-openobserve.service
    mode: "0644"

- name: Make sure openobserve service is running
  ansible.builtin.systemd_service:
    state: started
    name: docker-openobserve.service